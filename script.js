function lambext() {
    let lamb = document.getElementById('lamb')
    let por = document.getElementById('porsche')
    // LAMBORGUINI
    lamb.style.transition = "0.5s"
    lamb.style.height = "390px"
    lamb.style.top = "200px"

    // PORSCHE
    por.style.height = "495px"
    por.style.left = "1400px"
    por.style.top = "140px"
    por.style.transition = "0.5s"
}